(
  pricing_mode: CostAware,
  producer_margin_percent: 15,
  operating_reserve_ticks: 3,
  import_priorities: [
    (good: "core:energy", percent: 200),
    (good: "frontier:habitat_modules", percent: 110),
  ],
  liquidation_threshold_percent: 100,
  liquidation_discount_percent: 50,
  default_target: 10,
  reservation_ttl: 20,
  life_support_burn_per_capita: 1,
  source_output_percent: 100,
  idle_trader_repositioning: true,
  brownouts: (
    throttled_entry_ticks: 12,
    emergency_entry_ticks: 6,
    starvation_entry_ticks: 1,
    throttled_recovery_ticks: 16,
    emergency_recovery_ticks: 8,
    starvation_recovery_ticks: 3,
    minimum_stage_ticks: 1,
    throttled_throughput_percent: 50,
    emergency_throughput_percent: 0,
    starvation_throughput_percent: 0,
    emergency_energy_bid_ceiling: 10,
    survival_goods: ["core:energy"],
  ),
  population: (
    static_population: false,
    sufficiency_window: 500,
    growth_sufficiency_percent: 90,
    essential_goods: ["core:energy"],
    tertiary_demand: [
      (good: "frontier:habitat_modules", units_per_thousand: 1),
    ],
    decline_per_thousand: 10,
    growth_per_thousand: 1,
    logistic_scale: 1,
    minimum_cap: 0,
    maximum_cap: 1000000,
    tier_thresholds: [1, 5, 10],
  ),
  investments: [
    (kind: Collector, enabled: true, base_cost: 4000, cost_growth_percent: 150, maximum_level: 4, cooldown_ticks: 100, effect_per_level: 2),
    (kind: Storage, enabled: true, base_cost: 4000, cost_growth_percent: 150, maximum_level: 4, cooldown_ticks: 100, effect_per_level: 500),
    (kind: PopulationSupport, enabled: true, base_cost: 4500, cost_growth_percent: 175, maximum_level: 4, cooldown_ticks: 200, effect_per_level: 2),
    (kind: RouteSubsidy, enabled: true, base_cost: 4500, cost_growth_percent: 175, maximum_level: 4, cooldown_ticks: 100, effect_per_level: 10),
  ],
  default_investment_allocation: [
    (kind: Collector, percent: 30),
    (kind: Storage, percent: 25),
    (kind: PopulationSupport, percent: 25),
    (kind: RouteSubsidy, percent: 20),
  ],
)
